# Copy to .env or set these environment variables.
# The service also reads ../.env.local (shared with the Vite app),
# which already has DATABASE_URL pointing at your Tailscale hostname.

# Override if needed (otherwise reads from ../.env.local):
# DATABASE_URL=postgresql://postgres:postgres@ethan-pc-1.tail5ea3c.ts.net:54322/postgres

OPTIMIZER_PORT=3001
RUST_LOG=uma_optimizer=info
